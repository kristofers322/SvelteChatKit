FROM nginx:alpine

# Copy the generation script
COPY generate-html.sh /generate-html.sh

# Make the script executable
RUN chmod +x /generate-html.sh

# Use the script as the entrypoint
ENTRYPOINT ["/generate-html.sh"]
